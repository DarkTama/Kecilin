{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-extras",
  "description": "Windows-only capabilities (drag-out plugin)",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": ["drag:default"]
}
